export {
  MIN_PITCH,
  MIN_RATE,
  MIN_VOLUME,
  MAX_PITCH,
  MAX_RATE,
  MAX_VOLUME,
  DEFAULT_PITCH,
  DEFAULT_RATE,
  DEFAULT_VOLUME,
} from '@shayc/async-speech';

export { useSpeech, SpeechProvider } from './speech-context';
